# Leave empty for local dev (Vite proxies /api to localhost:8000)
# For production, set to your Render backend URL (no trailing slash):
# VITE_API_URL=https://housefinance-api.onrender.com
VITE_API_URL=
